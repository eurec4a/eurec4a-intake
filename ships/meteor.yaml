sources:
  LICHT_LIDAR:
    description: Observations made with the LICHT LIDAR on R/V Meteor
    args:
      path: "{{CATALOG_DIR}}/LICHT_lidar.yaml"
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
