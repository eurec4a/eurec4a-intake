
sources:

  meteor:
    description: Observations from the R/V Meteor ship
    args:
      path: "{{CATALOG_DIR}}/meteor.yaml"
    driver: intake.catalog.local.YAMLFileCatalog
    metadata:
      website: http://eurec4a.eu/platforms/rv-meteor
