plugins:
  source:
     - module: intake_xarray

sources:
  ICON:
    args:
      path: "{{CATALOG_DIR}}/ICON/main.yaml"
    description: 'ICON simulations'
    driver: yaml_file_cat
    metadata: {}       
