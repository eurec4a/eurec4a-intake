plugins:
  source:
     - module: intake_xarray

sources:
  experiment_1:
    args:
      path: "{{CATALOG_DIR}}/experiment_1.yaml"
    description: 'Experiment with higher aerosol concentrations'
    driver: yaml_file_cat
    metadata: {}
  experiment_2:
    args:
      path: "{{CATALOG_DIR}}/experiment_2.yaml"
    description: 'Experiment with realistic aerosol concentrations'
    driver: yaml_file_cat
    metadata: {}
